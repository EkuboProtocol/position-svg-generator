export const ratio = 4;

export const SVG_WIDTH = 1_000 / ratio;
export const SVG_HEIGHT = 1_000 / ratio;

export const SVG_GLOBAL_PADDING = 30 / ratio;
export const SVG_TEXT_X_PADDING = 42 / ratio;
export const SVG_TEXT_Y_PADDING = 22 / ratio;

export const SVG_MAIN_FONT_SIZE = 94 / ratio;
export const SVG_SMALLER_FONT_SIZE = 64 / ratio;

export const SVG_INNER_RECT_RADIUS = 18 / ratio;
export const SVG_INNER_RECT_STROKE_WIDTH = 1 / ratio;

export const SVG_BOUNDS_FONT_SIZE = 36 / ratio;

export const SVG_IMAGE_STROKE_WIDTH = 10 / ratio;
export const SVG_IMAGE_SIZE = SVG_MAIN_FONT_SIZE;

export const SVG_IMAGE_Y_OFFSET = SVG_MAIN_FONT_SIZE * 0.13;

export const SVG_GRADIENT_GRADIENT_RECT_WIDTH = 300 / ratio;
export const SVG_GRADIENT_GRADIENT_RECT_HEIGHT = 140 / ratio;
